apiVersion: v1
data:
  start.sh: |+
    #!/bin/sh
    echo "=== Startup Script Running ==="
    echo "App: ${APP_NAME}"
    echo "Mode: ${APP_MODE}"
    echo "Env snapshot:"
    env | sort
    echo "=== Ready ==="
    tail -f /dev/null

kind: ConfigMap
metadata:
  name: start-sh-cfg
  namespace: backend
